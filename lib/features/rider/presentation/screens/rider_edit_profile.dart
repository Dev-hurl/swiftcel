import 'package:flutter/material.dart';

class RiderEditProfile extends StatelessWidget {
  const RiderEditProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}