import 'package:flutter/material.dart';

class CreateDeliveryParcelScreen extends StatelessWidget {
  const CreateDeliveryParcelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}