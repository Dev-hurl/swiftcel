import 'package:flutter/material.dart';

class FindingRider extends StatelessWidget {
  const FindingRider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}